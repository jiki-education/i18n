---
title: "散个步"
description: "使用能走多步的函数在迷宫中穿行。"
en_md5: 91ee3e60f948f052d7551029f5d6fdf7
---

我们又回到迷宫中啦！上次你在这里时，你使用了 `move()`（移动）函数，一次移动一步。这可行，但连续写很多次 `move()` 实在有点枯燥！

在这个练习中，我们用一个新的函数（英文：function）取代了 `move()`，这个函数叫做 `walk()`（行走）。这个函数接受一个数字作为输入（英文：input），并向前移动相应的步数。例如，`walk(3)` 向前移动 3 步，而 `walk(1)` 仅向前移动 1 步。

在之后的所有练习中，你都会看到类似下方的“函数”部分，方便你清楚了解有哪些函数可用以及它们如何工作。

使用 `walk()`、`turnLeft()`（左转）和 `turnRight()`（右转）在迷宫中穿行，到达绿色的目标格子。
