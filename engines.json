{
  "$comment": [
    "Which LLM translates. Never hardcoded at a call site: scripts/translate.mjs",
    "reads this, and --engine/--model override it for a one-off run.",
    "`byLocale` overrides the default for one locale, the same way the translator",
    "repo's languages/<lang>/tracking.json `translation_engine` block does."
  ],
  "default": {
    "engine": "anthropic",
    "model": "claude-opus-4-5"
  },
  "byLocale": {}
}
